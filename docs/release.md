# Release Notes

## 发布经验

- 命令系统有新增、删除或改名时，必须同时检查并同步 `skills/` 下的说明文档与参考文件。
- 如果命令示例、场景文档、cheatsheet 仍引用旧命令名，Agent 会优先学到错误用法，所以文档同步不是可选项。
- 发版前至少做三件事：
  - 校验 help 输出与实际命令注册一致。
  - 校验 skills/reference 与 README 中的示例命令仍可执行。
  - 校验构建或打包产物没有遗漏最新命令。
